# Copy to web/.env.local for development.
# Base URL of the deployed LIQO API Worker. Leave UNSET to run the PWA fully
# offline (the same pure engine runs client-side against the bundled sample).
# VITE_API_BASE=https://liqo-api.<your-subdomain>.workers.dev
